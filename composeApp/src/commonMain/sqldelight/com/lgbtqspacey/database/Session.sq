CREATE TABLE IF NOT EXISTS Session(
    token TEXT NOT NULL PRIMARY KEY DEFAULT "",
    expiration TEXT NOT NULL DEFAULT "",
    userId TEXT NOT NULL DEFAULT ""
);

insertSession:
INSERT OR REPLACE INTO Session(token, expiration, userId)
VALUES (?, ?, ?);

getSession:
SELECT * FROM Session;

deleteSession:
DELETE FROM Session;
