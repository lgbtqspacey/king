CREATE TABLE IF NOT EXISTS Settings (
    isDarkTheme TEXT DEFAULT "true"
);

insertSettings:
INSERT OR REPLACE INTO Settings(isDarkTheme)
VALUES (?);

getSettings:
SELECT * FROM Settings;

deleteSettings:
DELETE FROM Settings;
