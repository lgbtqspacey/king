CREATE TABLE IF NOT EXISTS Settings (
    isDarkMode TEXT NOT NULL DEFAULT "true",
    autoUpdate TEXT NOT NULL DEFAULT "true"
);

updateisDarkTheme:
INSERT OR REPLACE INTO Settings(isDarkMode)
VALUES (?);

getSettings:
SELECT * FROM Settings;

deleteSettings:
DELETE FROM Settings;
